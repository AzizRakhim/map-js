/*
let arr = [1, 2, 5, 6, 8];
let num = 2;

function arrMultiply(arr, num){
  let newArr = [];
  for(let i = 0; i < arr.length; i++){
    newArr[i] = arr[i] * num;
    console.log(arr[i]);
  }
  console.log(newArr);
}

arrMultiply(arr, num);
*/

/*
let arr = [20, 10, 8, 23, 33];
let num = 18;

function biggerThan(arr, num) {
  let newArr = [];
  for(let i = 0; i < arr.length; i++){
    if(arr[i] >= num){
      newArr.push(arr[i]);
    }
  }
  console.log(newArr);
}

biggerThan(arr, num);
*/

